<div class="row">
  <div class="col-md-12 alert-secundary text-center">

    <% flash.get('mensaje').forEach(function (msg) { %>
    <div class="alert alert-info text-center" role="alert">
      <%=msg%>
    </div>
    <% }) %>

    <h2>Mis Reservas</h2>

    <table class="table">
      <thead>
        <tr>
          <th scope="col">Recurso</th>
          <th scope="col">Fecha</th>
          <th scope="col">Tiempo</th>
          <th scope="col">Pedido</th>
        </tr>
      </thead>
      <tbody>
          <% reservas.forEach( reserva => { %>
            <tr>
            <td scope="row"><a href="/mis-reservas/<%=reserva.id%>"><%=reserva.nombre%></a></td>
            <td scope="row"><%=reserva.fecha%></td>
            <td scope="row"><%=reserva.tiempo%></td>
            <td scope="row"><%=reserva.pedido%></td>
          </tr>
          <% }) %>
        </tbody>
    </table>

  </div>
</div>
